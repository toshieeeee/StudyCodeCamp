<?php 

//定数定義 → 変数定義 → 関数実行 → 関数定義 
//大まかに、こんな感じの処理の流れになっている。
//多分だけど。require()で、
//関数は、別ファイルにまとめといた方がいいのではないかと思う。

//定数を定義

//定数は、大文字で定義するのがルールぽい。
//difine('定数名','値')

/***********************************
▼DB Access INFO
************************************/

define('DSN','mysql:dbname=dbname;host=localhost')
define('DB_USER','username');
define('DB_PASSWD', 'passwd');

/***********************************
▼文字コード定義
************************************/


//DBに接続する関数

//→これに関しては、try{}catch + PDOでやった方が、デバックしやすいような感じがする。

/*
$dsn = 'mysql:dbname=testdb;host=127.0.0.1';
$user = 'dbuser';
$password = 'dbpass';

try {
    $dbh = new PDO($dsn, $user, $password);
    $dbh->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch (PDOException $e) {
    echo 'Connection failed: ' . $e->getMessage();
}
*/

/***********************************
▼クエリの結果を、配列で取得する関数
************************************/

/***********************************
▼商品の一覧を取得する関数
************************************/



?>